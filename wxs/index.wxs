function handlePrice(price, discoutPrice){
    return discoutPrice ? discoutPrice : price
}

function handleTag(tags){
    return tags.split('$')
}

module.exports = {
    handlePrice: handlePrice,
    handleTag: handleTag
}