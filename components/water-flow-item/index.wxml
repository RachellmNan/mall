<view class="container">
    <image src="{{img}}" mode="widthFix" />
    <view class="content">
        <view class="title">
            <text >{{title}}</text>
        </view>
        <view class="tabs" wx:if="{{tags}}" >
            <m-tag title="{{item}}" wx:for="{{list}}" wx:if="{{list.length > 0}}"  wx:key="index" />
        </view>
        <text class="price">¥ {{price}}</text>
        <view class="desc">
            <text>{{desc}}</text>
        </view>
    </view>
</view>