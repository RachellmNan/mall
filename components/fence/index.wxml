<view class="container">
    <text class="title">{{fence.title}}</text>
    <view class="cell-wrapper">
        <m-cell  x="{{x}}" y="{{index}}" cell="{{item}}" wx:for="{{fence.cells}}" wx:key="index"></m-cell>
    </view>
</view>